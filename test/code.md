The following are some examples for `Code`.

---

## Code

Inline `code`

line code

``line of code``

Block code "fences"

```
Sample text here...
```

Syntax highlighting

``` js
var foo = function (bar) {
  return bar++;
};

console.log(foo(5));
```
